apiVersion: v1
kind: Service
metadata:
  name: community
  namespace: travel-planner
spec:
  selector:
    app: community
  ports:
    - name: http
      port: 8080
      targetPort: 8080
    - name: actuator
      port: 9091
      targetPort: 9091
